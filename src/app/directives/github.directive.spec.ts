import { GithubDirective } from './github.directive';

describe('GithubDirective', () => {
  it('should create an instance', () => {
    const directive = new GithubDirective();
    expect(directive).toBeTruthy();
  });
});
